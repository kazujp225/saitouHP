services:
  - type: web
    name: saitou-hp
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: RESEND_API_KEY
        sync: false
      - key: CONTACT_EMAIL
        sync: false
